%h3 New comment
= form_tag blogpost_comment_path(record.id), :method => 'post' do
  = label_tag(:title)
  = text_field_tag(:title)
  = label_tag(:comment)
  = text_field_tag(:comment)
  = submit_tag("Add")
